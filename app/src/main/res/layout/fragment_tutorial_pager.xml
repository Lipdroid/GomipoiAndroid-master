<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/match_parent_width"
    android:layout_height="@dimen/tutorial_pager_height"
    android:background="@color/white">
    <FrameLayout
        android:id="@+id/layoutDescript"
        android:layout_width="match_parent"
        android:layout_height="290dp">
        <TextView
            android:id="@+id/textViewDescript"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:textSize="@dimen/sp16"
            android:textColor="@color/dialog_text"/>
    </FrameLayout>

    <ImageView
        android:id="@+id/imageViewTutorial"
        android:layout_width="320dp"
        android:layout_height="200dp"
        android:layout_below="@id/layoutDescript"
        android:contentDescription="@null" />
</RelativeLayout>